<section class="channels-list">
  <h1>Ada Channels</h1>
  <% if @channels %>
    <ul>
      <% @channels.each do |channel| %>
        <li>
          <%= link_to channel.name, chat_new_path(channel.id) %>
        </li>
      <% end %>
    </ul>
  <% end %>
</section>
